server {
    listen 3000;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: fall back to index.html so client routes resolve.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
