description: "Security scan for ops-crew"

prompts:
  - "{{message}}"

providers:
  - id: http
    config:
      url: http://localhost:8000/health
      method: GET

redteam:
  plugins:
    - prompt-injection
    - jailbreak
    - pii
